---
title: Physics-informed Reinforcement Learning for Stochastic Reach-Avoid Analysis
title_zh: 面向随机可达-规避分析的物理信息强化学习
authors: "Hikaru Hoshino, Yorie Nakahira"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.17117v1"
tags: ["query:rl-motion"]
score: 9.0
evidence: 面向动态系统安全关键控制的物理信息强化学习
tldr: 随机到达-避免分析是安全关键控制的重要工具，其核心是求解Hamilton-Jacobi偏微分方程，但随着系统维度升高，传统数值方法和标准PINN分别面临计算爆炸与局部极小问题。本文提出物理信息强化学习（PIRL）框架，通过调度策略先利用时序差分actor-critic学习获得有价值函数近似，再渐进加入PDE残差和边界条件损失以强制一致性，从而弥补各自不足。案例研究表明该方法精度可与成功训练的PINN相媲美，同时显著提升鲁棒性，为高维随机控制提供可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17117v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 920, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 818, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 824, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 733, \"height\": 814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 918, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 916, \"height\": 437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17117v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 917, \"height\": 419, \"label\": \"Figure\"}]"
motivation: 高维下随机到达-避免PDE难以求解，传统数值法不可行，纯PINN易陷局部极小，纯RL则缺乏PDE一致性。
method: 先用时序差分actor-critic学习逼近价值函数，再渐进引入PDE残差与边界条件损失进行约束修正。
result: 缓解了标准PINN的失败模式，在案例中达到与成功训练PINN相当的精度。
conclusion: PIRL结合两者优势，为高维随机安全分析提供有效可扩展工具。
---

## 摘要
受控动力系统的随机可达-规避分析是在不确定性下进行安全关键控制的重要工具，其中可达-规避概率由Hamilton-Jacobi偏微分方程(PDE)刻画。然而，随着系统维度的增加，使用传统数值方法求解该PDE在计算上变得难以处理。物理信息神经网络(PINNs)在主要通过PDE残差最小化进行训练时，可能收敛到不准确的局部最小值。强化学习(RL)提供了一种可扩展的替代方案，但其学习的价值函数可能不准确或与主控PDE不一致。本文提出了一种物理信息强化学习(PIRL)框架，该框架结合了PINNs和RL的互补优势，用于随机可达-规避分析。我们开发了一种调度PIRL算法，其中时序差分演员-评论家学习首先引导评论家获得对可达-规避价值函数的有意义近似。然后逐步引入PDE残差和边界条件损失，以强制与主控PDE及其边界条件保持一致。所提出的方法减轻了传统PINN技术的失败模式，同时实现了与成功训练的PINN相当的准确性。通过两个案例研究证明了所提出框架的有效性。

## Abstract
Stochastic reach-avoid analysis of controlled dynamical systems is an important tool for safety-critical control under uncertainty, in which the reach-avoid probability is characterized by a Hamilton-Jacobi partial differential equation (PDE). However, solving this PDE using conventional numerical methods becomes computationally intractable as the system dimension increases. Physics-informed neural networks (PINNs) may converge to inaccurate local minima when trained primarily through PDE-residual minimization. Reinforcement learning (RL) offers a scalable alternative, but its learned value functions may be inaccurate or inconsistent with the governing PDE. This paper proposes a physics-informed RL (PIRL) framework that combines the complementary strengths of PINNs and RL for stochastic reach-avoid analysis. We develop a scheduled PIRL algorithm in which temporal-difference actor-critic learning first guides the critic toward a meaningful approximation of the reach-avoid value function. PDE-residual and boundary-condition losses are then introduced progressively to enforce consistency with the governing PDE and its boundary conditions. The proposed method mitigates the failure modes of conventional PINN techniques while achieving accuracy comparable to that of successfully trained PINNs. The effectiveness of the proposed framework is demonstrated through two case studies.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究问题**：随机可达-规避分析（Stochastic Reach-Avoid Analysis）面向受控随机动态系统，旨在刻画系统在给定时间范围内、以高于某一阈值的概率先到达目标集、并避开规避集的能力。该概率由 Hamilton-Jacobi-Bellman (HJB) 型偏微分方程（PDE）刻画。
- **面临的挑战**：
  - 传统数值方法（如网格法）在高维状态空间下计算量呈指数增长，难以扩展；
  - 物理信息神经网络（PINN）虽然避免了离散化，但若仅通过 PDE 残差最小化训练，容易收敛到不准确的局部极小值甚至近似平凡解（“伪满足”残差但价值函数无意义）；
  - 强化学习（RL）可从采样轨迹中学习策略和值函数、具有可扩展性，但标准 RL 仅依赖时序差分（TD）监督，难以保证学习到的值函数与主控 PDE 一致。
- **核心目标**：提出一种物理信息强化学习（PIRL）框架，融合 RL 的采样优势与 PINN 的 PDE 约束优势，实现高维随机可达-规避概率的准确、可靠求解，并避免纯 PINN 训练的失败模式。

## 2. 方法论

### 2.1 核心思想

- **提出一种“先轨迹学习、后物理约束”的分阶段训练策略**：
  1. 先用 TD actor-critic 学习获得较合理的可达-规避价值函数近似，引导策略优化；
  2. 随后逐步引入 PDE 残差损失和边界条件损失，强制价值函数满足 HJB 方程及其边界条件。
- 通过 Sigmoid 调度函数（式 43-44）动态调整三类损失的权重，使训练从“RL 主导”平滑过渡到“PDE 主导”。

### 2.2 关键技术细节

- **RL 等价形式转换**（Proposition 1）：
  - 原始可达-规避概率是 max-multiplicative 成本结构，不兼容标准 RL 的加性累积奖励；
  - 通过将“剩余时间”加入状态、并使目标集 A 和规避集 B 成为吸收集，构造增广状态 `Y = [τ, x]`；
  - 定义仅在剩余时间落在 `[0, Δt)` 时给出指示目标奖励的奖励函数，将原概率等价地表示为折扣因子为 1 的无限 horizon 累积奖励（RL 值函数）。
- **连续时间极限**（Theorem 1）：
  - 当 `Δt → 0` 时，平滑后的 RL 值函数收敛到线性/非线性抛物型 PDE 的唯一古典解，对应 HJB 方程的时间反转形式。
- **混合损失设计**（式 36-41）：
  - TD 损失：基于 TD3 目标网络计算；
  - PDE 残差损失：在状态-时间域上采样配置点，计算算子 `L^u` 残差的平方；
  - 边界损失：在抛物型边界 `{0}×O_ε ∪ (0,T]×∂O_ε` 上强制值函数等于平滑后的边界函数 `l_ε(x)`。
  - 总评论家损失为三者加权和；每次更新后按 Sigmoid 调度调整权重。
- **算法流程**（Algorithm 1）：
  - 并行 rollout 收集轨迹存入回放缓冲；
  - 每步采样小批量过渡数据、配置点和边界点；
  - 更新评论家（混合损失）、定期更新演员（确定性策略梯度）和目标网络；
  - 按调度函数更新各损失权重。
- **理论误差保证**（Corollary 1）：
  - 将 PINN 误差分析应用于策略评估 PDE，得到价值函数误差可由 PDE 残差和边界失配控制的界，为物理约束提供理论依据。

## 3. 实验设计

### 3.1 场景 1：一维随机可达问题

- **系统**：`dX_t = U_t dt + dW_t`，`U_t ∈ [-1,1]`，目标集 `A = {x ≥ 2}`，无规避集，时间范围 `T=2`。
- **基准**：该问题有解析解（漂移布朗运动首达概率）。
- **对比方法**：
  - TD3（纯强化学习）；
  - PINN-PI（PINN 策略迭代）；
  - 提出的 PIRL。
- **评估指标**：平均奖励（策略性能）、价值函数 MSE（相对解析解的误差）。
- **设置**：每个方法跑 10 个随机种子，比较成功/失败次数和误差分布；还绘制学习曲线、热力图比较学习到的值函数与真实值。

### 3.2 场景 2：车辆漂移可达-规避控制

- **系统**：8 维时间增强状态空间，状态为 `[e_y, e_ψ, v_x, v_y, r, δ, μ]` 加上剩余时间 `τ`；控制输入为转向率 `δ̇` 和纵向力 `F_x`；使用 Fiala 轮胎模型和非线性路径相对动力学。
- **目标/规避集**：目标为漂移平衡点附近的邻域；规避集表示失控状态（如道路偏离、过度横摆、失速、转向饱和、摩擦系数越界等）。
- **训练流程**：
  - 先用 TD3 在课程学习下训练（先小扰动局部，再扩大初始状态分布）；然后从 TD3 检查点继续 PIRL 训练（过渡阶段 + 精化阶段）。
  - 使用 LLM 引导的外层循环自动调整调度参数（如权重、配置点分布）。
- **对比方法**：
  - 固定初始 horizon 的 TD3（10M 更新）；
  - 随机初始 horizon 的 TD3（10M）；
  - PIRL 过渡结束（10M）和精化后（15M）。
- **评估指标**：
  - 验证 HJB 残差损失和边界损失；
  - 与蒙特卡洛模拟得到的经验可达-规避概率比较计算 MSE，并按评估点距 TD3 轨迹支撑的距离分为最近、近、中、远、最远五组；
  - 绘制 β-r 平面和 e_y-e_ψ 平面的值函数切片与闭环 rollout 轨迹。

## 4. 资源与算力

- **论文未明确说明**使用何种 GPU、GPU 数量、训练时长等硬件信息。
- 仅提及训练更新步数（如 2×10^5、10^6、5×10^6、10^7、15M），以及并行 rollout workers（但未给具体数值）。
- 因此无法从文中获取详细的算力开销和计算资源信息。

## 5. 实验数量与充分性

- **数量**：
  - 一维实验：10 个随机种子 × 3 种方法，包含奖励、MSE 和损失曲线分析；
  - 车辆实验：多阶段训练（TD3 两个阶段 + PIRL 过渡/精化），对比四种配置，并分五组空间距离评估价值精度。
- **充分性**：
  - 优点：一维实验统计了随机种子的成功/失败分布，揭示了 PINN-PI 失败模式；损失演变分析（HJB、边界、TD）表明单纯 PDE 残差小不一定代表值函数正确；车辆实验通过多维投影和 rollout 验证了 learned value 的物理合理性。
  - 不足：仅两个案例，且车辆案例缺少与传统网格法或求解器（如水平集法）的定量对比；没有对调度参数、不同 RL 算法（如 DQN/DDPG/PPO）进行消融；车辆实验依赖课程学习和 LLM 引导，外部干预较多，可重复性和公平性有待增强。
- **客观性**：使用了蒙特卡洛估计值和解析解作为基准，指标明确，但 PIRL 与 TD3 的对比中 PIRL 是在 TD3 预训练基础上继续训练，起点不同，比较时需注意公平性。

## 6. 主要结论与发现

- **一维例**：
  - PIRL 在所有 10 个种子中均获得近最优奖励，而 PINN-PI 有 6/10 次失败（收敛到低奖励的平凡解）；
  - PIRL 的价值函数 MSE 与成功训练 PINN-PI 相当，优于纯 TD3；
  - 关键发现：失败的 PINN-PI 的 HJB 损失和边界损失并不比成功的大，但 TD 损失显著偏大——即 PDE 残差小可能对应错误价值函数，TD 监督可识别和规避这种失败。
- **车辆漂移例**：
  - PIRL（15M）比 TD3 基线具有更低的 HJB/边界损失；
  - 在远离 TD3 轨迹支撑的区域，PIRL 的价值函数 MSE 最低，表明其外推能力更强；
  - 学习到的可达-规避值函数在相平面切片上形成围绕漂移平衡点的高值区，并正确反映闭环系统在完整耦合状态下的可恢复性（出现单投影无法判断的失败机制）。
- **总体**：结合 TD 轨迹监督和 PDE 约束的 PIRL 能兼顾策略学习鲁棒性和价值函数准确性，为高维随机可达-规避分析提供可扩展方案。

## 7. 优点

- **理论完备**：严格证明了 RL 值与可达-规避概率的等价性（Proposition 1）、连续时间极限到 HJB 解的收敛性（Theorem 1），并给出基于 PDE 残差的误差界（Corollary 1），为方法提供扎实依据。
- **方法创新**：提出了“先 TD 后 PDE”的调度机制，针对 PINN 残差训练的失败模式设计，逻辑清晰且可操作性强。
- **实践价值**：将 TD3 扩展到随机可达-规避问题，并通过 8 维车辆漂移应用展示了超出常规 PINN 可处理维度的能力。
- **自动调参**：引入 LLM 引导的外层循环自动调度损失权重和配置点分布，减少人工调参工作量。
- **结果可视化**：通过多种图（热力图、损失曲线、相图、rollout）直观展示了学习价值函数的质量和物理一致性。
- **开源**：提供了源代码和数值结果，利于复现。

## 8. 不足与局限

- **实验规模有限**：只有一维基准和车辆漂移两个案例，缺少更高维、不同类型（如电网、多智能体）的验证；未与现有随机可达性工具箱或水平集数值解进行定量对比。
- **依赖启发式/外部指导**：车辆实验中使用了课程学习、针对不同阶段的采样分布设计以及 LLM 调度，这些外部组件可能影响方法的普适性和公平性，且 LLM 调度引入的随机性未被系统分析。
- **调度参数敏感性**：Sigmoid 调度的中心、陡度、权重初末值等对结果影响较大，文中未给出系统的敏感性分析。
- **理论假设较强**：误差界依赖古典解存在性、Lipschitz 连续性、均匀采样等假设，实际系统可能不满足；定理 1 要求 Assumption 1/2（非退化性、光滑边界等），限制了适用范围。
- **未讨论计算成本**：没有报告训练时间、内存消耗、GPU 需求，实际部署时的效率无法评估。
- **基线选择较窄**：对比只集中在 TD3 和 PINN-PI，未与 DDPG、DQN、SAC、其他 PIRL 变体或基于证书/下界的方法比较；价值误差比较时 TD3 基线未做相同尺度的 PDE 约束增强，可能不完全公平。

（完）

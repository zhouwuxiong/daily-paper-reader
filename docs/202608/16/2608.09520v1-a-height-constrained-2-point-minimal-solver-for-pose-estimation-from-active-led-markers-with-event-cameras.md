---
title: A Height-Constrained 2-Point Minimal Solver for Pose Estimation from Active LED Markers with Event Cameras
title_zh: 一种用于事件相机主动LED标记姿态估计的高度约束两点最小求解器
authors: "Runze Yuan, Alexander Kappler, Jun Zhang, Kuangyi Chen, Fabio Morbidi, Pascal Vasseur, Cédric Demonceaux, Friedrich Fraundorfer"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09520v1"
tags: ["query:d-slam"]
score: 8.0
evidence: 事件相机与主动标记的极小位姿求解
tldr: 针对事件相机与主动LED标记的实时定位，现有PnP求解器依赖多个标记且空间受限。我们提出结合IMU倾斜角与高度计的高度约束两点最小求解器，仅需两个LED标记即可通过闭式或线性最小二乘求解位姿，并分析退化情形。实验证明精度优于P2P，与P3P相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09520v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1836, \"height\": 1103, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1829, \"height\": 1066, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 892, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 890, \"height\": 371, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09520v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 492, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09520v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 479, \"label\": \"Table\"}]"
motivation: 现有PnP求解器需要多个标记，结构化布局在空间受限场景部署困难，且未利用已有的倾斜角与高度信息。
method: 引入已知相机倾角与高度作为额外约束，推导仅用两个LED标记的闭式与线性最小二乘位姿求解器，并分析退化构型。
result: 在合成与真实事件相机数据上，所提求解器精度优于现有P2P，且性能与P3P相当。
conclusion: 高度约束可有效减少所需标记数量并提升位姿估计精度，适合资源受限的自主定位应用。
---

## 摘要
在许多需要实时定位的自主应用中，与计算密集型的基于特征的方法相比，基于主动标记的系统因其低延迟和易于部署而受到青睐。事件相机提供高时间分辨率和最小延迟，通常与主动LED标记一起使用，以实现鲁棒的实时定位。现有方法通常依赖透视n点（PnP）求解器进行姿态估计。然而，在空间受限的场景中，结构化标记布局可能难以部署，而来自机载传感器的部分自运动信息（例如重力方向和高度）是现成可用的。我们推导出一种鲁棒且精确的最小求解器，该求解器通过结合机载传感器（如IMU或高度计）测量的已知倾斜角和相机高度，仅从两个LED标记即可估计相机姿态。所提出的公式通过闭式解和线性最小二乘解唯一确定相机姿态。我们进一步分析了退化配置，并刻画了高度信息对旋转估计无贡献的条件。为了评估，我们开发了一个基于事件的主动标记系统，以使用运动捕捉系统的地面真值收集真实世界数据。在合成数据和真实数据上的实验表明，与最先进的P2P求解器相比，所提出方法具有更高的精度，且相对于P3P具有竞争力的性能。

## Abstract
In many autonomous applications requiring real-time localization, active marker-based systems are preferred due to their low latency and ease of deployment compared to computationally demanding feature-based methods. Event~\mbox{cameras} offer high temporal resolution and minimal delay and are commonly used with active LED markers for robust real-time localization. Existing methods typically rely on Perspective-n-Point (PnP) solvers for pose estimation. However, structured marker layouts can be challenging to deploy in space-constrained scenarios, while partial self-motion information (e.g., gravity direction and altitude) is readily available from onboard sensors. We derive a robust and accurate minimal solver that estimates camera pose from only two LED markers by incorporating known tilt angle and camera height measured by an onboard sensor, such as an IMU or an altimeter. The proposed formulation uniquely determines the camera pose through both a closed-form and a linear least-squares solution. We further analyze degenerate configurations and characterize the conditions under which height information does not contribute to rotation estimation. For evaluation, we developed an event-based active marker system to collect real-world data with ground truth from a motion capture system. Experiments on both synthetic and real data demonstrate improved accuracy over the state-of-the-art P2P solver and competitive performance relative to P3P.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- 许多自主机器人系统在GNSS受限环境（室内、隧道）中需要快速、精确的相机姿态估计。
- 传统PnP方法依赖大量3D-2D特征匹配，计算开销大，难以满足实时性要求；基于主动标记（如LED）的定位系统具有低延迟、易部署、无需预先建图的优势。
- 事件相机具有高时间分辨率、高动态范围，适合追踪主动闪烁的LED标记，在快速运动和极端光照下表现优于传统相机。
- 现有基于事件的标记定位系统通常需要至少3个LED标记（P3P求解）才能确定唯一姿态，且P2P求解器（如Li et al.）会给出多个解，需要额外的点来消除歧义，增加了硬件复杂度和部署难度。
- 在多数机器人平台上，倾斜角（重力方向）和高度信息可以从IMU/高度计等传感器直接获得，但现有方法未充分利用这些先验信息。
- 本文的核心动机是：利用已知的倾斜角和高度先验，仅用两个LED标记即可唯一确定相机6-DoF姿态，从而降低标记数量、简化部署，同时提升精度和鲁棒性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 将已知垂直方向（倾斜角）和高度作为额外约束，重新构造P2P（两点）位姿求解问题，使得未知量从6-DoF降为3-DoF（一个旋转角 + 两个平移分量），从而仅用两个点即可获得唯一解。

### 关键建模
- 相机与机器人本体系通过已知外参 T_cb 关联；
- 世界坐标系下两个LED标记的3D坐标 P1, P2 已知；
- 通过IMU获得垂直方向，将旋转分解为 tilt 旋转（RxRy）和绕z轴的旋转（Rz）；
- 通过高度计获得相机/机器人高度 h，结合外参得到 t′ 的z分量为常数 -h′；
- 得到待求参数向量 [cosθ, sinθ, t′_x, t′_y]^T，满足线性系统 `Ax = b`（4个方程），并受三角函数恒等式约束。

### 两种求解策略
1. **闭式解（Closed-Form Solution）**
   - 利用点对坐标差分（ΔX, ΔY）和测量值（α1, α2）消去平移项，得到两个关于 s = tan(θ/2) 的独立二次方程；
   - 通过判别式选择更良态的方程求解旋转角，另一方程作为独立约束消除歧义，得到唯一解；
   - 再代入原线性系统求平移。

2. **线性最小二乘解（Linear Least-Squares Solution）**
   - 暂时忽略三角函数约束，对 Ax = b 用SVD求最小二乘解；
   - 将解出的 Rz 投影回SO(3)（SVD后令 Rz = UV^T）。

### 退化分析（理论贡献）
- 空间退化：
  - 两点平面坐标相同（仅高度不同）→ 系统秩亏，无解；
  - 两点高度相同 → 线性LS解中高度信息仅作为尺度因子，旋转估计退化为无高度约束的Li et al.方法；但闭式解仍可利用高度信息。
- 运动退化：
  - 相机高度与某一点高度相同（Zi - h′=0）时，对应方程退化；若两点高度均与相机相同，则b=0，仅平凡解。
- 实际建议：选择水平分离大、高度有差异的标记，且相机与标记保持足够高度差。

## 3. 实验设计：数据集 / 场景 / benchmark / 对比方法

### 合成数据实验
- 相机位姿采样：以标记质心为球心、不同半径的球面上采样1000个位姿，加入姿态噪声（roll/pitch 1°）和高度噪声（0.03m）模拟传感器误差；
- 实验一（像素噪声影响）：固定距离10m，像素噪声σ=0~5px；
- 实验二（距离影响）：固定σ=2px，距离1~30m；
- 实验三（退化配置验证）：
  - 两标记高度同为3m（水平坐标不同），相机高度变化0~10m；
  - 两标记高度分别为3m和4m，相机高度变化0~10m；
- 对比方法：
  - P3P [Kneip et al. 2011]
  - P2P [Li et al. 2023]（现有SOTA两点求解器）
  - 本文P2P（闭式解）
  - 本文P2P（LS解）

### 真实数据实验
- 硬件系统：
  - 事件相机：Prophesee EVK4 HD（IMX636ES, 1280×720）
  - 镜头：Soyo SFA0820-5M
  - 主动目标：三个高功率LED（频率1000Hz, 1150Hz, 850Hz），由ESP32单片机PWM控制
- 地面真值：OptiTrack运动捕捉系统，提供相机位姿和3D LED位置；事件相机通过手眼标定与OptiTrack坐标系对齐；相机内参由Prophesee SDK标定。
- LED检测流程：20ms时间窗累积事件 → 空间DBSCAN聚类 → 时间域频率估计 → 平均事件像素坐标得到观测点。
- 数据内容：4条轨迹序列
  - Seq1, Seq2：任意运动；
  - Seq3：近距离运动（1.25–2.75 m）；
  - Seq4：远距离运动（3.4–4.6 m）；
- 注意：P3P和Li et al.方法多解，评估时选择相对地面真值误差最小的解；本文方法给唯一解，更符合实际使用。
- 对比方法与合成实验相同。

## 4. 资源与算力

- 论文全文 **未提及任何GPU型号、数量、训练/推理时长、算力资源** 等详细信息。
- 方法为解析/线性求解器，计算量极小，无需训练；硬件为嵌入式级（event camera + ESP32 + LED），但具体计算平台（是否使用GPU）未说明。
- 可推断该方法主要面向实时轻量应用，不依赖大规模算力。

## 5. 实验数量与充分性

### 实验数量
- 合成实验：3大组（噪声、距离、退化），每大组含多个条件（像素噪声0-5px，距离1-30m，相机高度0-10m），每次采样1000位姿，统计中位数/均值/标准差。
- 真实实验：4条轨迹序列，对比4种方法（含本文两种变体），报告位置和旋转误差的均值、标准差、中位数。

### 充分性评价
- **优点**：合成实验覆盖了主变量（噪声、距离、退化配置），距离和噪声范围较大；退化实验有针对性验证；真实实验包含近距和远距场景，具有实际参考价值。
- **不足**：
  - 未提供消融研究（例如高度噪声、倾斜角噪声对结果的独立影响？）；
  - 真实实验中地面真值高度和倾斜角直接送给求解器（实验条件理想化），未评估机载传感器实际精度对姿态估计的影响；
  - 未评估动态场景下的时间连续性（轨迹随时间变化的误差趋势）；
  - 硬件实验中仅4条序列，统计显著性有限；
  - 对Li et al.和P3P的解选择采用了“最优解”策略，这在真实部署中不可行（通常需要额外点/RANSAC），可能使对比略偏保守（对自己更不利），但总体还是公平的。

## 6. 论文的主要结论与发现

- 利用高度先验可以将P2P问题的解唯一化，不需要额外的第三个点来消除歧义。
- 合成实验中：
  - 在噪声较大或距离较远时，所提P2P的**平移精度显著优于P3P和Li et al.**；
  - 旋转精度与Li et al.相当，两者均优于P3P（高噪声/远距离条件下）；
  - 短距离/低噪声时，P3P位置精度略优，但所有P2P方法差距不大。
- 闭式解与LS解性能接近，LS在旋转噪声鲁棒性上略好；闭式解对旋转噪声更敏感。
- 退化分析得到验证：当相机高度等于某个标记高度（垂直方向无差异）时，所提方法病态；当两标记等高时，LS解退化为无高度约束的形式（Li et al.），而闭式解仍能利用高度信息。
- 真实实验中：
  - 所提P2P在平移和旋转精度上一致优于Li et al.的P2P；
  - 与P3P相比，位置误差相当或略优，旋转误差略高但方差更小（数值稳定性更好）；
  - 在远距离序列（Seq4）中优势更明显（位置误差降低约45%对比Li et al.）；
  - 唯一解特性使其在实际应用中更具吸引力。

## 7. 优点

- **创新性**：首次将高度先验纳入P2P求解器，实现了仅用两点、无需额外点即可唯一确定姿态，理论上比传统两点解更简洁。
- **数学完整性**：同时给出闭式解和线性LS解，并进行了深入的退化分析（空间配置 + 相机运动），给出明确退化条件，为实际系统部署提供了指导。
- **鲁棒性**：远距离、高噪声环境下位置精度优于SOTA P2P和P3P，且方差更小。
- **实用性**：降低标记数量（由3个降为2个）和硬件成本；方法可直接用于嵌入式/实时系统；提供了完整的硬件实现和检测流水线（DBSCAN+频率估计）。
- **实验设计较全面**：合成+真实、多变量分析、退化验证、多指标（平移/旋转误差的均值/中位数/标准差），对比公允（对baseline采用最优解选择）。

## 8. 不足与局限

- **传感器假设的理想化**：真实实验中，倾斜角和高度直接采用地面真值输入求解器，未考虑IMU漂移、高度计噪声等实际情况，可能高估了方法在真实系统中的精度。
- **退化情况的存在**：当相机高度等于标记高度或两标记高度接近/相同、坐标差分小时，方法会病态或失效，限制了部分应用场景（如室内平面无人机）。
- **旋转精度优势不明显**：相比P3P，在近距/低噪时旋转误差稍差；相比Li et al.的P2P，旋转精度无实质提升（高度先验主要改善平移）。
- **对比策略存在偏差风险**：P3P和Li et al.的多解均选择了与真值最接近的解，这在实际在线系统中不可实现（需RANSAC或额外点），因而真实性能可能比报告中略低（此偏差有利于baseline，但也意味着本文方法在真实对比中可能更优越）。
- **实验统计有限**：真实数据仅4条序列，且没有提供每个序列的时长/帧数；无跨场景（不同光照、不同地面、动态遮挡）验证。
- **未讨论与视觉惯导融合的互补性**：高度和倾斜角来自IMU/高度计，但没有分析传感器失效或异常时的容错策略。
- **未开源代码/数据集**：论文未明确提供代码或数据，复现性受限。

（完）

---
title: "Jetson-ORB-SLAM3: Accuracy-Preserving GPU Implementation for Edge Computing Devices"
title_zh: "Jetson-ORB-SLAM3: 面向边缘计算设备的精度保持型GPU实现"
authors: "Rajat Roy, Aditya Arun Kumar Yadav, Hardik Jain"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17874v1"
tags: ["query:d-slam"]
score: 9.0
evidence: 面向边缘设备的视觉惯性SLAM，GPU ORB前端与CNN闭环
tldr: "低功耗边缘平台上的视觉惯性SLAM受特征提取与回环检测开销制约。现有GPU移植常牺牲精度。本文提出面向Jetson Orin Nano的保精度GPU实现，GPU前端与CPU参考算法达94.7%关键点一致率，并原生TensorRT加速CNN回环。EuRoC上四种配置轨迹误差差异小于0.10cm，单目惯性模式达32FPS。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17874v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1029, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17874v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 709, \"height\": 748, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17874v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 753, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 602, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1562, \"height\": 826, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 811, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 915, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 596, \"height\": 580, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 808, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 730, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17874v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 829, \"height\": 547, \"label\": \"Table\"}]"
motivation: 边缘设备上GPU加速SLAM通常损失精度，需在保持轨迹一致性的前提下提升前端与回环性能。
method: 将ORB特征提取前端移植到GPU并保持算法一致性，CNN回环用原生TensorRT FP16引擎加速，后端仍留CPU。
result: EuRoC上GPU与CPU轨迹误差差≤0.10cm，TUM-VI五种序列亚厘米精度，回环推理2.2ms（180倍加速），单目惯性32FPS。
conclusion: 证明GPU移植可无损精度，使学习型回环在7W低功耗设备上实时运行，适合边缘SLAM部署。
---

## 摘要
低功耗边缘平台上的视觉惯性SLAM受限于密集特征提取与回环检测的成本。以往ORB-SLAM的GPU移植通过近似ORB检测器来换取速度，从而改变了特征集，进而影响估计轨迹。我们提出了一种用于NVIDIA Jetson Orin Nano的精度保持型ORB-SLAM3 GPU实现，其GPU ORB前端在算法层面复现了参考CPU检测器，达到94.7%的精确关键点一致率和99.9%的描述子位一致率。本工作还通过原生TensorRT使基于CNN的回环检测在边缘端变得可行。视觉前端（特征提取）被卸载到GPU，而建图与优化后端保留在CPU上，使每个计算任务匹配其适合的硬件。通过比较四种配置来验证精度：GPU流水线和未修改的CPU参考，每种均在Jetson Orin Nano和桌面上运行。在EuRoC数据集上，四种配置的平均绝对轨迹误差（SE(3)）均在0.10厘米以内，因此GPU移植和硬件更换都不会改变估计轨迹。GPU与CPU的对比在TUM-VI和KITTI数据集上可复现，表明这种加速是保持精度而非近似。所提出的实现在EuRoC上与已发表的ORB-SLAM3具有竞争力，在六个TUM-VI室内序列中的五个上达到亚厘米精度，在十一个KITTI序列中的九个上达到低于1%的相对平移误差。对于回环检测，通用的ONNX-Runtime CUDA/TensorRT执行提供程序在嵌入式平台上无法与我们的CosPlace ResNet-50一起使用，而原生libnvinfer FP16引擎将每次查询的推理时间降至2.2毫秒，实现了180倍的加速。因此，学习型地点识别可以在7瓦设备上与跟踪并行运行。在单目惯性模式下，系统在十一个EuRoC序列上维持平均32FPS。

## Abstract
Visual-inertial SLAM on low-power edge platforms is constrained by the cost of dense feature extraction and loop closure. Prior GPU ports of ORB-SLAM trade accuracy for speed by approximating the ORB detector, altering the feature set and therefore the estimated trajectory. We present an accuracy-preserving GPU implementation of ORB-SLAM3 for the NVIDIA Jetson Orin Nano, whose GPU ORB front end reproduces the reference CPU detector algorithmically to 94.7% exact keypoint agreement and 99.9% descriptor bit agreement. This work also makes CNN-based loop closure edge-viable through native TensorRT. The visual front end (feature extraction) is offloaded to the GPU while the mapping and optimization back end is kept on the CPU, matching each computation to the hardware it suits. The accuracy is verified by comparing four configurations: the GPU pipeline and the unmodified CPU reference, each run on both the Jetson Orin Nano and a desktop. On EuRoC dataset, all four agree to within 0.10cm in mean absolute trajectory error (SE(3)), so neither the GPU port nor the change of hardware shifts the estimated trajectory. The GPU-versus-CPU comparison is reproducible on TUM-VI and KITTI datasets, so the acceleration is accuracy-preserving rather than approximate. The proposed implementation is competitive with published ORB-SLAM3 on EuRoC, attains sub-centimeter accuracy on five of the six TUM-VI room sequences, and reaches sub-1% relative translation error on nine of eleven KITTI sequences. For loop closure, the generic ONNX-Runtime CUDA/TensorRT execution providers are unusable with our CosPlace ResNet-50 on the embedded platform, whereas a native libnvinfer FP16 engine reduces per-query inference to 2.2ms, a 180x speedup. Learned place recognition therefore runs concurrently with tracking on a 7W device. In monocular-inertial mode the system sustains 32FPS mean over the eleven EuRoC sequences.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机与背景：**

- 视觉惯性SLAM（如ORB-SLAM3）是自主机器人、MAV在GPS拒止环境中导航的关键能力，但其计算开销大，尤其在低功耗边缘设备上难以实时运行。
- 现有GPU移植方案（如Jetson-SLAM）通常通过**近似**ORB检测器（如简化FAST响应或NMS）来换取速度，导致检测到的**特征集发生变化**，从而影响后端数据关联、束调整与最终轨迹精度——即“以精度换速度”。
- 此外，基于CNN的学习型回环检测（如CosPlace、NetVLAD）虽然检索性能优于词袋模型（DBoW2），但其深度网络前向推理在嵌入式平台上开销过大，很难集成到实时SLAM循环中。
- 本文的核心目标是：在NVIDIA Jetson Orin Nano这一约7W的低功耗设备上，实现一个**保持轨迹精度**的GPU版ORB-SLAM3，同时让CNN回环检测在边缘端变得实时可行。

## 2. 论文提出的方法论

**核心思想：**
“等价优先”设计——GPU前端**复现参考CPU检测器的算法**而非近似，使得GPU前端成为CPU提取器的“即插即用”替代品，后端（建图与优化）完全不变，从而继承ORB-SLAM3原有的精度。

**关键技术细节：**

1. **精度保持的GPU ORB前端**（CUDA实现，共5个阶段）：
   - 尺度金字塔：可分离高斯模糊+降采样，逐层目标特征数按参考分配。
   - FAST检测：FAST-9角点测试，保留低纹理单元格的备用低阈值策略，匹配参考的单元格占用规则。
   - 多尺度NMS：保留参考的网格均匀化与角点评分排序，而非随意取强角点子集。
   - 方向计算：基于强度质心法，使用参考的圆形掩码。
   - Steered rBRIEF描述子：按关键点方向旋转学习到的BRIEF采样模式，生成与参考一致的256位描述子。

2. **实时CNN回环检测**：
   - 使用CosPlace ResNet-50提取512维全局描述子，经L2归一化后以余弦相似度检索候选关键帧。
   - 设置时间间隔δ=20帧排除近期邻居。
   - 自适应候选门控阈值：τₖ = clip(μₖ + 3σₖ, τ_min, 0.95)，基于历史相似度分布动态调整，抑制感知混叠。
   - 候选经ORB SearchByBoW几何验证（≥10个内点）后进入标准Sim(3)/SE(3)回环校正路径。
   - 将CNN检索与DBoW2匹配结合：CNN负责高召回粗检索，DBoW2保证精度。

3. **Native TensorRT部署路径**：
   - 发现ONNX-Runtime的CUDA/TensorRT执行提供程序在Orin Nano上初始化失败（子图构建在ARM CPU上耗时数小时）。
   - 绕过ONNX-Runtime，离线编译ONNX模型为设备特定的TensorRT FP16引擎，运行时反序列化一次，在专用CUDA流上执行。
   - 每查询2.2ms，较CPU（~396ms）加速180倍。

## 3. 实验设计

**数据集与基准：**

| 数据集 | 模态 | 评估指标 |
|--------|------|----------|
| EuRoC（11个序列） | 立体惯性 / 单目惯性 | ATE（SE(3)与Sim(3) RMSE） |
| TUM-VI（6个room序列） | 鱼眼立体惯性 | ATE（SE(3) RMSE，身体坐标系） |
| KITTI（11个odometry序列） | 立体视觉（无IMU） | 相对平移误差t_rel、相对旋转误差r_rel、ATE |

**对比方法：**
- **2×2矩阵**：{Orin Nano, 桌面} × {GPU流水线, 未修改CPU参考}，用于区分移植影响与硬件影响。
- 与已发表ORB-SLAM3数据对比。
- 与Jetson-SLAM（近似GPU移植）同设备对比。
- 回环消融：DBoW2+CNN、DBoW2单独、无回环共3种配置。

**关键实验协议细节：**
- 统一使用evo工具进行身体坐标系对齐（避免镜头与IMU杆臂偏移导致ATE虚高）。
- 同时报告SE(3)（无尺度）与Sim(3)（尺度校正）结果。
- EuRoC两组均为5次运行取中位数。

## 4. 资源与算力

**文中明确给出的硬件与环境信息：**
- 目标设备：NVIDIA Jetson Orin Nano，6核ARM Cortex-A78AE，Ampere GPU，8GB共享内存，JetPack 6.2（L4T R36, CUDA 12.6, TensorRT 10.3）。
- 桌面参考平台：未给出具体型号/GPU。
- 功耗：稳定跟踪时6.3W平均、6.9W峰值（VDD_IN轨，tegrastats 250ms采样），空闲基线4.7W。
- TensorRT引擎构建：一次性步骤，约30秒，需在目标设备上构建。

**文中未明确说明：**
- 桌面平台的GPU型号、CPU型号等具体规格。
- 模型训练时长（CosPlace ResNet-50为预训练模型，未涉及训练）。
- 未提及实验所用GPU数量。

## 5. 实验数量与充分性

**实验数量概览：**
- EuRoC：11个序列，2种模态（Mono-Inertial与Stereo-Inertial），4种配置（2×2矩阵），5次运行取中位数，另有回环消融（3配置×5次）。
- TUM-VI：6个room序列，单次运行，GPU与CPU各一组。
- KITTI：11个odometry序列，单次运行，GPU与CPU各一组。
- 性能测试：每种模式每个序列的FPS数据（表7），三个数据集的平均跟踪时间对比（表8），逐阶段耗时拆分（表6）。
- 平台对比：EuRoC上4配置的跨平台等效性测试。

**充分性与客观性评估：**
- **优点**：多数据集、多模态、多平台交叉验证；统一评估工具和身体坐标系对齐；区分GPU移植效应与硬件效应（2×2矩阵是很好的设计）；回环消融实验严格（55对运行匹配中CNN劣于DBoW2下者为28次，中位数差异−0.01cm）。
- **局限**：TUM-VI和KITTI均为单次运行，未给出运行间方差；V203序列存在双峰分布，单序列均值报告可能误导读者的判断；与已发表ORB-SLAM3数据对比时，发表数据的运行条件（协议、运行次数、坐标系）可能不完全一致。

## 6. 论文的主要结论与发现

1. **特征级一致性**：GPU与CPU提取器在EuRoC 205帧上达到94.7%精确关键点一致率与99.9%描述子位一致率（平均Hamming距离0.25/256位），残差来自浮点舍入和平局裁决，而非算法差异。
2. **轨迹级等效性**：EuRoC上四种配置（GPU/CPU × Orin/桌面）平均ATE跨度仅0.10cm；Orin上GPU与CPU的逐序列平均差为0.27cm。TUM-VI上为0.18cm，KITTI上为0.036个百分点。
3. **性能**：
   - 单目惯性模式：EuRoC上均值32 FPS，所有序列实时。
   - 立体惯性模式：基线14.4 FPS，通过将ORB提取与跟踪重叠（pipeline）提升至28.0 FPS，超过所有序列的20Hz相机帧率。
   - 与CPU参考相比，GPU前端在KITTI分辨率（1241×376）下快11.5%，在EuRoC分辨率（752×480）下反而更慢——收益取决于每帧计算量与内核启动开销之比。
4. **CNN回环**：原生TensorRT FP16推理2.2ms/查询，较CPU加速180倍；在EuRoC上精度中立（对55对运行匹配中位数差异−0.01cm），回环的主要受益序列是V202（误差减半）。
5. **与近似移植的对比**：同设备上Jetson-SLAM平均ATE 13.6cm vs 本工作3.3cm，差距主要出现在需要惯性融合的MH03–MH05序列上。

## 7. 优点

- **“等价优先”的设计理念**：不追求GPU上的“更好”检测器，而是严格复现CPU参考算法，使得精度保证成为设计目标而非事后验证。
- **多层次的等效性验证**：从特征级（关键点一致率、描述子位一致率）到轨迹级（2×2矩阵、跨数据集），证据链完整。
- **跨数据集的可复现性**：EuRoC、TUM-VI、KITTI三套基准、三种不同场景特征（室内、鱼眼、自动驾驶），并设计跨数据集统一相对误差评估方法（表5），使三种不同量纲的基准可比较。
- **严谨的评估协议**：注意身体坐标系对齐细节、Sim(3)与SE(3)同时报告、多运行中位数统计、区分仪表化构建与未仪表化构建。
- **诚实报告边界条件**：GPU在较小帧上并不比CPU快、后端的束调整不适合GPU卸载、V203序列运行间变异大于移植差异——这些“负面”观察增加了报告的可信度。
- **实际工程问题披露**：ONNX-Runtime执行提供程序在嵌入式的失败原因分析细致，体现边缘部署特有的约束。

## 8. 不足与局限

- **实验覆盖偏斜**：TUM-VI和KITTI为单次运行，缺少运行间方差数据，无法评估这两个数据集的统计稳定性。
- **与发表数据对比的不完全公平**：已发表ORB-SLAM3数据来自桌面、协议可能不同（运行次数、对齐方式、坐标系），表1和表3的对比仅供参考；论文在MH04、MH05、V203等序列上误差高于发表数据的2倍左右，作者将原因归于惯性初始化与重力一致性保护机制，但对比本身仍不是严格同条件。
- **V203序列的统计处理**：V203呈双峰分布，均值与中位数差异大，将其纳入均值报告可能产生误导，单独列出更为合适。
- **CNN回环的泛化性未验证**：仅验证了EuRoC一个数据集上的精度中性，未在更具挑战性的外观/视角变化场景（如KITTI、TUM-VI）验证CNN回环的增益，作者也承认在EuRoC上“贡献是边缘可行性而非精度增益”。
- **能效分析不足**：功耗测量仅给出总体均值/峰值，没有对比GPU前端与CPU前端各自的功耗差异，也未给出能效比（FPS/W）指标。
- **应用边界未充分讨论**：未在真实MAV飞行数据上验证，也未测试长时间运行的地图增长与全局束调整对延迟的影响。
- **加速收益有条件性**：GPU前端仅在帧足够大时收益显著，在EuRoC分辨率的立体模式下反而较CPU更慢。对许多实际边缘应用（帧较小）来说，CPU上运行参考ORB方案可能就已足够。

（完）

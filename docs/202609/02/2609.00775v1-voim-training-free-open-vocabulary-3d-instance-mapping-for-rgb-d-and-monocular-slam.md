---
title: "VOIM: Training-Free Open-Vocabulary 3D Instance Mapping for RGB-D and Monocular SLAM"
title_zh: VOIM：用于RGB-D和单目SLAM的免训练开放词汇3D实例映射
authors: "Sangmin Song, Sarath Kodagoda, Marc G. Carmichael, Karthick Thiyagarajan, Amal Gunatilake, Kelly Prentice, Jodi Martin"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00775v1"
tags: ["query:d-slam"]
score: 9.0
evidence: 面向RGB-D与单目SLAM的开放词汇三维实例建图
tldr: 现有开放词汇3D实例地图系统常在首次检测时即确定实例与标签，证据薄弱易出错。VOIM提出无训练体素接地实例管理器，将实例与标签决策推迟至多视图软证据在体素中累积，并表明映射阶段而非感知模型决定最终性能。在ScanNet++上以统一协议达44.07 mIoU，远超最强RGB-D系统OVO-SLAM的32.37，且无需修改即可用于单目RGB。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.00775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1831, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1842, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1806, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00775v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 890, \"height\": 709, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.00775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 383, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 897, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 806, \"height\": 163, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00775v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 928, \"height\": 264, \"label\": \"Table\"}]"
motivation: 在线实例地图系统在首次检测时即提交实例与标签，证据不足导致地图质量下降，需要延迟决策策略。
method: VOIM以体素为基本单元跨视图累积离壳感知模型的软证据，延迟实例分割与标签决策，无需额外训练即可处理RGB-D与单目输入。
result: ScanNet++上达44.07 mIoU较OVO-SLAM领先11.7，单目Replica上与基线持平；但房间尺度受标签类别限制，建筑尺度受漂移影响。
conclusion: 映射阶段比感知模型更关键，通过软证据累积延迟标签决策即可大幅提升开放词汇实例地图质量，且训练无关。
---

## 摘要
我们提出了体素接地在线实例管理器（VOIM），一种免训练的体素接地实例管理器，可从RGB-D或仅单目RGB构建开放词汇3D实例地图，这是现有免训练系统尚未涉及的领域。在线系统通常在首次检测时分割对象实例并为其标注，在证据最弱时做出决策。VOIM则推迟标签和实例决策，直到来自未修改的现成感知的软证据在各视图间逐体素积累。我们表明，映射阶段而非特定感知模型承载了结果：在ScanNet++上的四种感知配置中，改变区域描述符、检测器标签先验和掩码来源，该地图比最强的在线RGB-D系统OVO-SLAM高出4.8至11.7个mIoU点。感知并非中性的，将该基线自身的描述符系列替换会消耗4.1的差额，而基线携带略优的2D描述符（三个场景上33.7对31.5 mIoU），仍实现了较差的地图。在同类协议下，VOIM在ScanNet++上达到44.07 mIoU，而基线为32.37，赢得全部十个场景和两种聚合方式（合并33.31对25.97），并且同一系统可不变地运行到完全单目RGB，匹配该基线在Replica上的合并结果（27.80对27.50）。该优势具有领域特异性：在Replica的全类别评分下，匹配输入给出分裂结果，合并为28.60对27.50，而每场景平均为24.59对30.11。房间尺度受标签限制，建筑尺度受漂移限制。标注无法实时运行，主要受全词汇类别的逐类检测影响。地图导出占用网格，并将自由形式的查询解析到对象实例。

## Abstract
We present Voxel-Grounded Online Instance Manager (VOIM), a training-free voxel-grounded instance manager that builds open-vocabulary 3D instance maps from RGB-D or from monocular RGB alone, a regime no prior training-free system addresses. Online systems typically segment object instances and label them at first detection, committing when evidence is weakest. VOIM instead defers label and instance decisions until soft evidence from unmodified, off-the-shelf perception has accumulated per voxel across views. We show that the mapping stage, rather than the particular perception models, carries the result: across four perception configurations on ScanNet++, varying the region descriptor, the detector label prior and the mask source, the map exceeds the strongest online RGB-D system, OVO-SLAM, by between 4.8 and 11.7 mIoU. Perception is not neutral, and substituting that baseline's own descriptor family costs 4.1 of the margin, yet the baseline carries the marginally better 2D descriptor (33.7 vs. 31.5 mIoU over three scenes) and still realizes the weaker map. Under a like-for-like protocol VOIM reaches 44.07 mIoU on ScanNet++ against 32.37, winning all ten scenes and both aggregations (pooled 33.31 vs. 25.97), and the same system runs unchanged to fully monocular RGB, matching that baseline pooled on Replica (27.80 vs. 27.50). The advantage is regime-specific: under Replica's all-classes scoring, matched inputs give a split result, 28.60 vs. 27.50 pooled against 24.59 vs. 30.11 on the per-scene mean. Room scale is label-limited and building scale drift-limited. Labeling does not run in real time, dominated by per-class detection over the full vocabulary. The maps export occupancy grids and resolve free-form queries to object instances.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义

- **问题背景**：现有在线开放词汇 3D 实例地图系统通常在**首次检测到对象时**就进行实例分割和语义标签分配，而这一时刻往往是证据最薄弱的时候，容易导致早期错误被固化到地图中，最终影响地图质量。  
- **研究动机**：作者希望探索一种更稳健的建图策略——将实例与标签的决策尽量“推迟”，直到来自多视图的感知证据充分累积；同时，这一过程应当是**免训练**的，即不依赖针对特定 SLAM/地图任务重新训练的感知模块。  
- **整体含义**：该工作试图说明，在开放词汇 3D 实例映射任务中，**“映射阶段”才是性能的决定性因素**，而不是过度依赖某个特定的感知模型；同时，该框架还能扩展到**仅单目 RGB**输入这一之前免训练方法尚未覆盖的场景。

## 2. 方法论

- **核心思想**：提出 Voxel-Grounded Online Instance Manager（VOIM，体素接地在线实例管理器）。与“首见即决策”的传统在线系统不同，VOIM 将标签和实例分配决策**延后到多视图证据累积完成之后**。  
- **技术细节**：
  - 地图以**体素（voxel）**为基本单位；
  - 感知侧使用**现成的、未修改的开放词汇感知模型**，产生逐视图的软证据（soft evidence），例如掩码和类别/描述符分布；
  - 这些软证据在**不同视角下逐体素累积**；
  - 标签和实例的最终判定基于累积后的证据，而非单帧观测。  
- **输入范围**：同一系统既可以处理 RGB-D 输入，也可以在不做任何改动的情况下处理纯单目 RGB 输入。  
- **输出**：导出为占用网格（occupancy grid），并支持将自由形式查询（free-form queries）解析到具体对象实例。  
- **训练属性**：完全免训练，无需为建图任务重新训练模型。  
- **关于公式或流程的说明**：原文摘要中未给出具体公式、网络结构或逐步伪代码，因此此处只能从摘要所述“跨视图软证据 + 体素累积 + 延迟决策”的行为层面进行概括。

## 3. 实验设计

- **数据集 / 场景**：
  - **ScanNet++**：用于 RGB-D 输入下的主评测；
  - **Replica**：用于单目 RGB 以及不同评分协议下的对比。  
- **基准与对比方法**：
  - 主要对照方法为当前最强的在线 RGB-D 开放词汇实例地图系统 **OVO-SLAM**；
  - 同时在 ScanNet++ 上评估了四种不同的感知配置（改变区域描述符、检测器标签先验和掩码来源）。
- **评测方式**：
  - 使用 mIoU（平均交互比）作为指标；
  - 同时报告了「合并（pooled）」和「每场景平均（per-scene mean）」两种聚合方式；
  - 在 ScanNet++ 上采用了“同类协议（like-for-like protocol）”，覆盖全部十个场景；
  - 在 Replica 上还对比了“全类别评分（all-classes scoring）”下不同聚合方式的结果。
- **额外对比**：在三个场景上比较了基线所携带的 2D 描述符（33.7）与 VOIM 使用的描述符（31.5），以说明感知描述符质量不是地图结果的决定性因素。

## 4. 资源与算力

- **文中未明确提及**使用的 GPU 型号、GPU 数量、训练时长等具体算力信息。
- 由于该方法是**免训练**的，因此不存在模型训练阶段的算力开销；
- 但文中明确指出：**标注（labeling）无法实时运行**，主要瓶颈在于需要在**完整词汇表上逐类进行检测**，这一计算开销影响了实时性。除此之外，没有提供额外硬件性能细节。

## 5. 实验数量与充分性

- **实验组数估计**：
  - 在 ScanNet++ 上包含 4 种感知配置的对比实验；
  - 主对比覆盖 10 个场景，并报告两种聚合方式；
  - 在 Replica 上进行单目 RGB 与 RGB-D/单目匹配输入的对比，且包含两种评分/聚合情形；
  - 另有 3 个场景的描述符质量对比。
- **充分性与客观性**：
  - 优点是采用了多个数据集、多种输入模态、多种感知配置以及不同聚合协议，并强调“同类协议”以保证对比公平；
  - 但也需要注意，由于不同数据集上的评分协议不同（如 Replica 上“全类别评分”时优势出现分裂），结论具有**领域/协议特异性**，不能简单外推到一切开放词汇建图场景；
  - 摘要中没有给出定性结果、可视化比较、逐类别的详细指标，也没有关于运行时间的具体量化实验，因此实验描述限于摘要层面，**完整性尚不够透明**。

## 6. 主要结论与发现

- **映射阶段比感知模型更关键**：在 ScanNet++ 上，无论感知配置如何变化，VOIM 的地图结果都显著优于 OVO-SLAM，提升幅度在 4.8 到 11.7 个 mIoU 点之间。
- **感知并非中性**：若将 VOIM 中的区域描述符替换为基线所用的描述符家族，优势会损失约 4.1 个 mIoU 点；说明感知特征仍有影响，但不足以改变整体优劣格局。
- **更强的 2D 描述符未必获得更好的地图**：基线的 2D 描述符在三个场景上略优（33.7 vs 31.5 mIoU），但其最终构建的地图仍然更差，从而印证建图决策的重要性。
- **主要量化结果**：
  - 在同类协议下，ScanNet++ 上 VOIM 达到 **44.07 mIoU**，而 OVO-SLAM 仅为 **32.37 mIoU**；VOIM 赢得全部 10 个场景以及两种聚合方式（pooled 为 33.31 vs 25.97）。
  - 同一套系统可直接用于纯单目 RGB，在 Replica 上与基线合并结果持平（27.80 vs 27.50）。
  - 优势具有领域特异性：在 Replica 的全类别评分下，pooled 结果 VOIM 略优（28.60 vs 27.50），但 per-scene mean 上反而落后（24.59 vs 30.11）。
- **系统局限认知**：房间尺度地图主要受标签类别数量限制，建筑尺度地图主要受 SLAM 漂移限制。

## 7. 优点

- **清晰的动机**：指出“首次检测时立即提交”这一普遍性缺陷，并提出延迟决策的有效思路。
- **免训练**：无需为建图或实例分割重新训练网络，可直接使用现成感知模型。
- **跨输入模态兼容**：同一系统不做修改即可支持 RGB-D 和纯单目 RGB，覆盖面更广。
- **系统性对比**：给出了多种感知配置的对比，证明主要收益来自映射结构而非具体感知模型。
- **强调协议公平性**：采用同类评价协议，并用 pooled 和 per-scene mean 两种聚合方式分别报告，降低单一统计口径带来的偏差风险。
- **结果一致性**：在 ScanNet++ 上全部十个场景中均取得领先，说明方法在该域内具有较强的稳定优势。

## 8. 不足与局限

- **实验覆盖有限**：摘要只涉及 ScanNet++ 和 Replica 两个数据集，且没有列出不同类别的细粒度结果、也没有真实机器人/SLAM 漂移场景下的端到端验证。
- **优势域特异性**：在 Replica 全类别评分下，其每场景平均结果反而弱于基线，说明方法在特定数据分布或评价协议中并非普遍更好。
- **不可实时**：标注过程受完整词汇逐类检测的制约，无法实时运行，这会限制实际的在线 SLAM 应用场景。
- **尺度局限**：房间尺度受标签类别枚举方式限制，建筑尺度则受 SLAM 漂移制约，面向更大的环境时可靠性尚不明确。
- **算力与工程细节缺失**：未说明 GPU/CPU 环境、内存消耗、推理时间与硬件资源需求。
- **算法透明度不足**：摘要层面缺少对体素证据累积、实例分配与延迟决策的具体机制、超参数和伪代码描述，复现难度较大。

（完）
